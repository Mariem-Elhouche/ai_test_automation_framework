# =================================================
# 1?? Backoffice credentials
# =================================================
backoffice.user.email=mariem.elhouche-ext@noveocare.com
backoffice.user.password=Noveocare.2026**


# URL de l'application Back-Office
backoffice.url=https://stg-bo.noveocare.com/login
# =================================================
# 3?? API / Microservices
# =================================================
self.healing.enabled=true
self.healing.api.url=https://epigastric-troy-calculating.ngrok-free.dev/heal
self.healing.api.timeout=10
self.healing.debug.request=true
dashboard.api.url=http://127.0.0.1:8080
dashboard.api.key=change-me
dashboard.run.id=run-2026-05-13-regression

# Driver lifecycle
# false = new browser per scenario
# true  = reuse the same browser across scenarios (all tags)
driver.reuse.enabled=false

# When driver.reuse.enabled=true:
# full = reset cookies + storage + about:blank between scenarios (safer isolation)
# none = keep browser state between scenarios (faster but less isolated)
driver.reuse.reset.strategy=full
